name: order
columns:
  id:
    type: string
    flags: [id]
  bakery_id:
    type: string
    references: bakery
  client_id:
    type: string
    references: client
  is_deleted:
    type: bool
    flags: [hidden]
  created_at:
    type: datetime
sqlite:
  table: client_order
